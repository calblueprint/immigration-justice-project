import styled from 'styled-components';

export const SectionContainer = styled.section`
  box-shadow: 3px 4px 13px 4px rgba(0, 0, 0, 0.05);
  background: white;
  width: 100%;
  border-radius: 10px;
  display: flex;
  flex-direction: column;
  padding: 50px;
  gap: 40px;
  position: relative;
`;

export const Field = styled.div`
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 16px;
`;
